# 菜菜女仆配件实验室 · 项目总账

## v0.1.17（呆毛根锁与耳鳍间距约束；待真机验证）

- 以用户认定效果最好的 v0.1.16 为基线。该版的变小动作已确认正常；图层、静止校准、尾巴
  和缩放路径保持不变。v0.1.17 只处理头部左右大幅转动时的耳鳍间距，以及呆毛根部轻微偏移。
- 两只耳鳍各自读取独立的脸部网格锚点，减弱侧脸局部横向拉伸，并将过宽的双耳间距限制在
  稳定头部间距的 103%；透视造成的间距收窄仍允许，避免把双耳硬做成一体。
- 呆毛以 Sen `ArtMesh151` 确认的根部锚点逐帧跟随女仆头部，抵消来源模型自身造成的根部漂移。
  根部附近 10% 的六网格区域不给二次物理权重，之后平滑增大至末端；头部速度驱动有界弹簧，
  舞台缩放与拖动不作为惯性输入。这是运行时网格修正，最终视觉效果仍需真机检查。
- 远端 `main` 提交 `ab8112a`；Actions `35885269890` 构建成功，产物
  `Caicai-Maid-Live2D-Accessory-Lab-v0.1.17.apk`。重点复核左右大幅时双耳是否仍过宽、
  歪头与上下大幅时根部是否贴住头发、末端是否自然回弹，以及缩小/放大后有无突然甩动。

## v0.1.16（脸部三点独立绑定；已真机反馈）

- 呆毛和两只耳鳍分别固定到女仆原始脸部大网格的三个点；运动载体与已确认的绘制图层分开，
  改图层不会暗中改运动来源。耳鳍缩放响应已减弱，保留 Sen 原生双抖；尾巴路径未改。
- 用户反馈这是迄今效果最好的一版：歪头大摆恢复正常，变小后的耳鳍大小也正常。
  遗留问题是左右大幅转动时耳鳍间距过宽，呆毛有少量偏移，程度接近第一次成功绑定时。
- 远端提交 `a88b48f`；Actions `35877178346` 构建成功。后续绑定优化从本版出发，
  不回退到 v0.1.15 及更早的头部载体试验。

## v0.1.15（绘制层/载体解耦 + 头发素材节拆分）

- v0.1.14 真机与诊断确认失败原因。呆毛偏移 `-1` 后位于 `render order 627`，实际上在头饰
  `628–633` 后方；用户要求的是头饰画面前方一层，因此新默认阈值改为头饰最大 order `633`。
  旧逻辑还把载体换成 `Part16=头发阴影` 的 `ArtMesh99`；正确载体则是
  `ArtMesh363_Skinning/ArtMesh264_Skinning=顶部头发`。
- 呆毛的绘制插层和运动载体现已完全解耦：默认插层固定为头饰全部网格之后（画面前方一层）；无论用户怎样测试
  前后图层，根部都只读取顶部头发的固定三角。中立帧投影和已确认的缩放/X/Y 均不改变。
- v0.1.14 的“后发”递归组错误包含 `render order 0–371` 的三百多个网格，实际把左/右双马尾、
  多段后发和颜色分支全部吞成整体。该大组以及同类“前发”大组不再用于图层校准。
- 新候选层改用作者的蒙皮素材节 Part：后发左/右各段、左右双马尾与发丝、后发中间、顶部
  头发、编号头发层、侧发左右、散发等分别成为一步；同一素材节在颜色/样式分支中的 Part
  才合并。这里的 `texture_00–03.png` 是打包图集，不等于原始 PNG 图层，因此用蒙皮 Part
  比按整张 texture index 切分更接近原素材结构。
- 左右耳鳍继续独立选择层；非零偏移使用当前插层前侧素材节的对应屏幕侧固定三角。偏移零仍
  保留 v0.1.13 已验证的左右蝴蝶结载体，尾巴完全不改。
- 校准存储升级为 v3：呆毛新 `0` 直接表示头饰画面前方一层，不继承旧报告的 `-1`；
  全局、呆毛、耳鳍和尾巴的已确认默认缩放/X/Y数值保持原值。
- 诊断的每个素材节新增直属父 Part、render order、texture index 与 UV bounds，下一轮可直接
  锁定具体素材节，而不再依据递归大组猜测。

## v0.1.14（按 Part 粗粒度图层校准）

- 本轮只解决用户提出的“先由真机判断正确头发层，再决定最终载体”：不猜最终图层，不改
  v0.1.13 已确认的呆毛/双耳静止缩放和 X/Y，也不改已经通过的尾巴绑定。
- 新增明确方向的“往后（更容易被遮挡）/往前（更少遮挡）”按钮。呆毛单独保存一个图层偏移；
  耳鳍继续支持整对、画面左、画面右三个调节目标，左右可保存不同的图层偏移和数值反馈。
- 女仆头部只提供粗粒度逻辑 Part：后发、双马尾与后侧发、头顶发、脸、前发、头发阴影、
  两侧蝴蝶结、头饰。同一类金/粉/白/棕等颜色变体合并，不把单个 ArtMesh 暴露为一层。
- 主模型从固定“低层/高层”两段绘制改为按当前 render order 的多段绘制；配件插入所选 Part
  边界，女仆自身网格顺序不重排。偏移为 0 时，耳鳍仍在两侧蝴蝶结之前，呆毛仍在最前，
  因而默认画面与 v0.1.13 完全一致。
- 非零偏移用于同时预览载体是否吻合：呆毛读取当前插层后侧相邻 Part 的固定三角；每只耳鳍
  读取前侧相邻 Part 的对应屏幕侧固定三角。零偏移保留 v0.1.13 的头饰/左右蝴蝶结三角，
  防止首次启动位置变化。所有载体只读取几何，不继承 Part 透明度。
- “重置当前”同步把相应图层偏移归零；尾巴点击图层按钮只提示固定最后层，不写入无效数据。
- 位置诊断新增 `maid_part_layer_calibration`：完整记录逻辑 Part、合并后的 drawable 清单、
  前后插层、请求/实际槽位、render-order 阈值、相邻载体名称和实际固定三角，供下一轮固化。

## v0.1.13（蝴蝶结双侧透视挂件）

- v0.1.12 真机确认呆毛、耳鳍和尾巴的基础单向绑定成立：上下大幅更准确，“变小”三组均能
  同步缩放。剩余错误集中在左右侧脸：整张脸的统一二维相似变换会把两只耳鳍同向平移，不能
  表示一侧向镜头展开、另一侧向头部内收的拟三维透视。
- 女仆 `cdi3` 已确认存在 `Part30=蝴蝶结右`、`Part31=蝴蝶结左`。本版分别从两个 Part 选择
  固定局部三角，再依据中立帧 X 坐标分配为“画面左/画面右”，不直接相信编辑器左右命名。
- Sen 原生耳鳍九网格按中立模型 X 的最大间隔拆为两个过滤绘制 pass；没有复制或镜像网格，
  Sen 模型仍每帧只更新一次，`ParamL_angle/ParamR_angle/ParamR_angle2` 双抖链保持不变。
- 两侧耳鳍分别接收对应蝴蝶结局部三角的中立→当前变换；仅使用几何运动，不读取头饰 Part
  透明度。因此关闭女仆头饰不会隐藏耳鳍，耳鳍的显示/隐藏仍由独立 Sen 配件开关负责。
- 拆分后的两侧默认微调均为恒等值 `1.00/0.00/0.00/0°`，共同继承 v0.1.12 已确认的整对
  `1.24/-0.01/-0.29/整体+5°`，保证静止默认位置不因拆分而改变。
- 配件轮换仍严格只有“尾巴→呆毛→耳鳍”。耳鳍区新增独立调节目标“整对/画面左/画面右”，
  原缩放和 X/Y 按钮以及旋转按钮作用于当前目标，并显示、保存各自数值。
- 主模型分层阈值从“双马尾之后”改为“两侧蝴蝶结之前”：先画后侧主体，再画两只耳鳍，最后
  绘制蝴蝶结及更前层的头饰/头发，使蝴蝶结能够遮住耳鳍。图层关系不与头饰显隐绑定。
- 呆毛由整脸三角改为 `Part9=头饰` 的头顶局部三角，减少左右侧脸时的小幅中心漂移；已确认
  的尾巴身体载体完全不改。
- 诊断新增左右耳鳍实际 ArtMesh 清单、左右蝴蝶结固定顶点、独立微调值、图层策略及
  `ear_visibility_source=sen_accessory_only_not_headwear_opacity`。

## v0.1.12（女仆网格单向载体绑定）

- v0.1.11 真机结果确认未完成真正绑定：女仆左右大幅测试几乎不动，但呆毛/耳鳍大幅移动；
  歪头时可能反向也可能明显滞后；“变小”时耳鳍停在原地。由此确认 Sen 供体仍在接收不兼容
  的同名头身参数，之后的三角抵消无法可靠消除其骨架响应。
- 本版彻底删除女仆→Sen 的刚性头部、身体、手臂与衣摆参数复制。Sen 每帧从自身中立基线开始，
  只计算耳鳍双抖等配件局部动力；供体的头身骨架不再参与三个配件根节点运动。
- 呆毛和耳鳍读取女仆脸部固定三角、尾巴读取女仆裙身固定三角。三角从中立帧到当前帧的实际
  平移/旋转/缩放直接追加到各自已经校准好的配件 projection，是严格的女仆网格→配件单向
  载体，不再读取或抵消 Sen 脸/身体三角的当前运动。
- 临时左右测试改写女仆可见的 `ParamAngleX3`，上下测试使用 `ParamAngleY2`；完整动作参数表
  不在本轮猜测，后续由用户按肉眼结果逐项标注，用于自主待机。本轮唯一验收重点是配件根部
  是否随女仆实际网格同步移动及缩放。
- 诊断固定输出 `attachment_mode=maid_mesh_one_way_triangle_carriers`、
  `sen_rigid_parameter_drive=false` 与 `sen_local_accessory_dynamics=true`，便于真机报告确认没有
  回退到旧参数驱动路径。
- 保留三组独立筛选、原生双耳一次绘制、用户确认的零位与尾巴放大结果；没有恢复已删除的
  “呆毛+耳鳍”组合，也没有引入 Ruby。

## v0.1.11（正确模型基线 + 固定三角载体绑定）

- 当前唯一主模型是用户修改贴图的菜菜女仆原生 `maid.moc3`；Sen 仅作为三个配件的动力供体。
  当前源码不再加载、引用或以 Ruby 为绑定参考。v0.1.10 及以前关于 Ruby 的记录仅保留为失败
  试验史，禁止继续复用其判断和绑定路径。
- Sen 配件逻辑拆分保持干净：呆毛仅 6 个确认网格；耳鳍是 `Part113` 原生双耳参数链且只绘制
  一次；尾巴仅保留 `Part239` 中当前活动的可见网格。不存在“呆毛+耳鳍”组合筛选组。
- 女仆 `Part25=脸` 与 Sen `Part39=脸部` 分别选定一个固定 drawable 三角形，作为呆毛和耳鳍
  的共同头部载体；两组仍独立校准、独立隐藏、独立绘制。女仆 `Part29=裙子` 与 Sen
  `Part93=下装` 同样建立尾巴身体载体。
- 每帧先计算女仆载体从中立到当前帧的最小二乘相似变换，再抵消 Sen 载体自己的跨模型响应；
  三个配件只收到统一的最终 clip-space 修正，因此局部呆毛弯曲、双耳抖动和尾巴摆动不被抹掉。
- 载体使用固定 drawable 与固定三个顶点 ID，不再用会随动作改变极值的包围盒，也不再用只能
  表示位移的单点。诊断新增双方实际载体的 drawable、顶点 ID 和中立三角坐标。
- 保留最新确认零位：全局 `1.00/0.00/+0.20`；呆毛 `0.88/-0.03/-0.05`；耳鳍
  `1.24/-0.01/-0.29/整体+5°`；尾巴 `1.00/+0.02/-0.24`（已包含两次放大）。
- 新素材包统一为 `accessory-lab.json + caicai + sen-accessory`；Sen 仍使用原始编译模型，运行时
  仅绘制三配件并按需加载用户改色的 06/16/19 三张贴图。
- 真机结论：该方案仍失败。原因是三角修正之前仍把女仆同名头身参数送进 Sen，而同名参数在
  两个模型中的语义、幅度与方向并不兼容。此版本不得作为后续绑定参考。

以下内容为旧版试验记录，不代表当前架构。

## v0.1.10（呆毛根部单点绑定与头部大幅单轴测试，已废弃）

- v0.1.9 真机确认耳鳍已经正确同向跟随，仅有轻微位移；本轮将耳鳍视为已成功路径，不修改其
  参数映射、九网格筛选、整体旋转、双抖动作或 projection，避免把已通过的部分再次带坏。
- 呆毛仍左右反向，证明公共 Ruby→Sen 头部参数本身并未绑反，问题仅在 Sen 呆毛自身的根部
  响应。新增独立单点绑定：Ruby `Part25=脸` 的当前中心相对中立中心的位移，直接作用到 Sen
  已确认的 `ArtMesh151` 呆毛根部；完全不再读取旧的 `Part9=头饰` 或 `Part70=女仆发带`。
- 单点绑定只对呆毛六网格的独立绘制 pass 追加统一 clip-space 平移，不改变网格内部相对位置，
  因此根部跟随被校正，而尖端弯曲、缩放固化和 Sen 原生物理仍然保留。
- 新增 `头部左右大幅/上下大幅/歪头大幅` 三个单轴测试，分别只写满量程 `AngleX/Y/Z` 及其
  对应物理参数；原“综合头摆”保留。自动巡检扩展为三个头轴、身体、双臂五段循环。
- 静止校准和尾巴绑定全部不变；诊断将呆毛标记为
  `ruby_face_to_ahoge_root_translation`，耳鳍仍为 `shared_parameter_drive_native_rig`。

## v0.1.9（撤销错误头部框架，恢复原生参数跟随）

- 原始模型 `cdi3` 复核确认：v0.1.7/v0.1.8 的头部双点方向锚点并非稳定头骨部件，而是 Ruby
  `Part9=头饰` 与 Sen `Part70=女仆发带`；两套模型对这些装饰物有各自的透视和物理形变，不能
  用它们求同一个刚性头部相似变换。这才是头向右而呆毛/耳鳍向左的根因。
- 完整撤销 v0.1.8 的 X 增量反射，同时撤销 v0.1.7 对呆毛与耳鳍新增的双点 correction。头部
  两组恢复 v0.1.6 的 Ruby→Sen 兼容参数驱动，各自继续使用独立 filter、Sen 原生网格与物理；
  这不是未绑定，而是适用于两套不同拓扑模型的参数级绑定。
- 尾巴不经过上述错误头部路径，继续使用已经真机确认成功的 `Ruby Part29→Part26` 与
  `Sen Part93→Part83` 身体双点修正；位置 `1.00/+0.02/-0.24` 与原生摆动保持不变。
- 所有静止校准完整保留：全局 `1.00/0.00/+0.20`、呆毛 `0.88/-0.03/-0.05`、耳鳍
  `1.24/-0.01/-0.29/整体+5°`。三项选择器、原生双耳九网格、呆毛六网格均不变。
- 诊断明确记录呆毛/耳鳍为 `shared_parameter_drive_native_rig`、尾巴为
  `independent_body_frame_direct`，禁止后续再次把头饰或发带误作刚性头部锚点。

## v0.1.8（头部挂件水平跟随方向修正，已由 v0.1.9 撤销）

- v0.1.7 真机现象：女仆头部向右时，呆毛与耳鳍同步向左。两组都能稳定响应且方向一致，说明
  独立挂件修正已经执行，故障是 Ruby 头部参考 Part 的屏幕 X 增量与可见头部转向相反，并非
  未绑定、网格混组或左右耳再次绑反。
- 仅对呆毛和耳鳍的 Ruby 主模型头部姿势做“当前点相对中立点的 X 增量镜像”，Y 位移、缩放、
  旋转求解、Sen donor 自身姿势抵消和两组独立 projection 全部继续沿用。
- 尾巴复核结果：仍使用 `MAIN Part29→Part26` 与 `Sen Part93→Part83` 的身体双点框架，走直接
  方向且没有经过头部 X 修正；该路径在 v0.1.6 已保持成功，本轮不翻转、不改位置和原生摆动。
- 诊断中的 `attachment_groups` 明确标记头部两组为 `independent_head_frame_x_reflected`，尾巴为
  `independent_body_frame_direct`，便于下一次报告直接确认运行策略。
- 保留 v0.1.7 已确认的绑定零点、原生双耳九网格、呆毛六网格和三项配件选择列表。
- 真机结果仍然反向；结论作废。失败原因不是缺少一次水平翻转，而是头部方向锚点本身选错。

## v0.1.7（三配件独立挂件绑定）

- 用户真机确认 v0.1.6 的网格拆分和显示均正确；新诊断确认呆毛严格为 6 个网格，耳鳍为
  原生左右两组共 9 个网格：`ParamL_angle` 命中右侧 3 个，`ParamR_angle` 命中左侧 6 个，
  `manual_mirror=false`，因此本轮不再修改耳鳍识别与绘制结构。
- 固化最新绑定零位：全局 `1.00/0.00/+0.20`；呆毛 `0.88/-0.03/-0.05`；耳鳍
  `1.24/-0.01/-0.29/整体+5°`；尾巴 `1.00/+0.02/-0.24`（在上一份位置上放大两次）。
- 呆毛、耳鳍、尾巴分别使用自己的过滤器、校准 projection、中立姿势记录和挂件修正调用。
  呆毛与耳鳍虽然都跟随 Ruby 头部框架，但不会共享 drawable filter，也不会生成或绘制组合组；
  呆毛弯曲、左右耳抖动和尾巴摆动仍由各自 Sen 原生网格/物理负责。
- `上一件/下一件` 从枚举全量轮换改为显式三项列表：尾巴 → 呆毛 → 耳鳍。原先第四项是
  `GLOBAL` 整体校准，并非隐藏网格；现已彻底退出配件选择和显示/隐藏操作。
- 诊断新增 `attachment_groups` 和 `calibration_cycle`，明确三组独立绑定且
  `combined_group=false`；若后续某一配件跟随异常，可单独判断而不会与另一头部配件混淆。

## v0.1.6（Sen 原生双耳与呆毛独立驱动）

- 复核 Sen 原项目后确认：原生渲染器只绘制完整模型一次，并没有“复制左耳生成右耳”的逻辑。
  旧版依据当前 X 坐标拆左右耳、再镜像绘制右耳，才是耳鳍间距和旋转失控的根因。
- 耳鳍改为运行时依次拨动 `ParamL_angle/ParamR_angle/ParamR_angle2`，比较可见网格变化，
  从已确认的 `Part113` 六个种子网格扩展出完整原生双耳组；整组每帧只绘制一次。
- 呆毛继续严格限定为 `ArtMesh151/160/189/190/191/192`，不再与耳鳍共用二次头部挂点矩阵；
  两者只接收 Ruby→Sen 的兼容头部参数，各自保留 Sen 内部的变形器和物理响应。
- 尾巴已经通过真机验证，继续保留独立的身体双锚点修正，不改动其网格、动作与默认位置。
- 废弃旧耳鳍镜像产生的对称旋转和间距值，启用新的校准存储键；保留用户最后确认的三组
  X/Y/缩放默认值和耳鳍整组 -10° 校正。
- 修正整体舞台矩阵的乘法顺序：在 projection 后追加同一 clip 变换，使主模型和每个过滤绘制
  使用同一最终缩放/位移，避免整体调整时相对距离被二次放大。
- 诊断升级为 `caicai-maid-accessory-calibration-v2`，新增每个耳鳍参数实际命中的 ArtMesh、
  `manual_mirror=false` 与单次原生双耳绘制模式，便于下一次真机报告精确核验。

## v0.1.5（挂件坐标层与整体缩放轴心修复）

- 真机结果：v0.1.4 尾巴挂件连接成功；呆毛与耳鳍在整体调整时发生大幅漂移，整个女仆模型
  连续缩放时向右下角移动。
- 判定无需再次拆分网格：尾巴成功、头部两组可独立隐藏且耳鳍动力正常，证明三组 drawable
  筛选已经独立。问题来自屏幕空间修正被放进模型投影层后，又经过不同模型的布局矩阵二次换算。
- 新增模型矩阵共轭换算：整体舞台、双锚点挂件、右耳镜像、耳鳍成对旋转与间距全部在最终
  OpenGL clip 坐标层生效，再安全换回各模型的 projection，避免位移和旋转被重复放大。
- 整体缩放轴心从 Live2D 画布原点改为女仆可见立绘中心；主模型与三个配件共用同一轴心，
  缩放时不应再持续向右下角游走。
- 1.0 倍、整体位移为 0 时不执行额外舞台矩阵，保持已经确认的静止校准与尾巴位置不变。
- 诊断新增 `attachment_transform_space=post_model_clip` 及整体缩放轴心 `pivot_x/pivot_y`。

## v0.1.4（三配件双锚点挂件）

- v0.1.3 真机确认耳鳍手动双抖恢复正常后，正式启用三配件动态挂件连接。
- 呆毛与耳鳍共用独立的头部双锚点框架；尾巴使用腰胯/上身双锚点框架。每帧从中立姿势计算
  女仆主模型的平移、旋转和缩放变化，不再只做中心点位移。
- 同时计算 Sen donor 自身的刚性头部/身体框架并将其抵消，避免两套不同 Live2D 参数响应
  叠加造成漂移；框架刻意不取耳鳍、呆毛或尾巴网格，因此耳鳍双抖、呆毛弯曲和尾巴摆动等
  配件内部物理不会被挂点修正抹掉。
- 已确认的 v0.1.2 静止位置继续作为绑定零位；挂件算法在零位为恒等变换，不重新改动用户校准。
- 整体缩放与移动仍先统一应用到主模型和三个配件，再在屏幕空间求挂点修正，防止缩放后漂移、
  消失或重复套用模型矩阵。
- 诊断字段 `attachment_mode` 更新为 `dynamic_two_point_anchor`，便于区分旧固定校准版本。

## v0.1.3（耳鳍独立动力恢复）

- 修复“快速抖动两次”被错误发送给女仆主模型的问题；`Part113` 和兔耳物理属于 Sen
  配件模型，事件现优先路由到 `overlayModel`。
- 修复配件专用更新分支没有推进耳抖计时、也没有在物理计算前准备
  `ParamL_angle/ParamR_angle/ParamR_angle2` 隔离输出的问题。
- 新增仅供配件层使用的最小耳鳍人格循环：不运行 Sen 的表情、身体、触屏或完整待机，只保留
  已确认的两次短脉冲；首次约 24 秒，之后每 28～68 秒低频自主触发，完全静止时暂停。
- 手动按钮与自主反应共用 Sen 已确认的 135%速度、100%幅度和隐藏九轴→独立物理路线。
- 固化 v0.1.2 新诊断位置为重置/干净导入默认值：全局 Y=+0.20；呆毛
  `0.88/-0.02/-0.05`；耳鳍 `1.14/+0.01/-0.29/旋转+8°/间距+0.22/整体-10°`；
  尾巴 `0.96/+0.02/-0.24`。升级安装继续保留已有自动保存值。
- 挂件连接继续延后到耳鳍手动双抖与低频自主反应通过实机确认之后，避免把动力故障和锚点故障
  混在同一轮判断。


## v0.1.2（呆毛/耳鳍硬隔离）

- 复核首轮诊断发现：父级名称虽正确，但 `Part13/Part220` 的递归结果额外夹带十个
  `texture_16` 耳类网格；它们在首轮静止诊断中透明，动作切换后可能显形并被套用呆毛校准，
  从而表现为耳鳍与呆毛合成一个部件。
- 呆毛停止递归 Part 层级，只允许已确认的六段长呆毛网格：
  `ArtMesh151/160/189/190/191/192`。固化根点仍为 `ArtMesh151`，继续保留 Sen 已确认的
  56%整体、100%长度、83%宽度、-49°与模型局部 X=-0.006。
- 耳鳍停止递归邻近头部层级，只允许 `Part113=兔耳` 对应六个网格：
  `ArtMesh631/1095/1021/146/629/1019`；右侧仍由这一组围绕中心轴镜像。
- 增加“单独测试：耳鳍快速抖动两次”，完全复用原 Sen 135%速度、100%幅度的隔离物理路线，
  只混回 `ParamL_angle/ParamR_angle/ParamR_angle2`。若处于完全静止，按钮会先退出静止。
- 本轮不要求新的位置诊断；先用隐藏/显示与耳鳍双抖确认两个配件已经彻底分离。


## v0.1.1（首轮真机修复）

- 修复主模型分前后层绘制时复用并污染投影矩阵的问题；该问题会让第二遍的前层头发重复套用
  模型变换，表现为头顶蓝块、左侧游离发丝，并可能在整体放大后离开裁剪区或黑屏。
- 配件校准位移随整体缩放同比变化，避免呆毛在默认倍率正确、整体缩放后发生相对漂移。
- 固化首轮诊断原始值：全局 `Y -0.01`；呆毛 `0.88 / X -0.02 / Y -0.05`；尾巴
  `0.82 / X 0.00 / Y 0.00`。最终合计位置分别为呆毛 `Y -0.06`、尾巴 `Y -0.01`。
- `Part113` 的六个耳鳍网格全部位于左侧，右侧并非遮挡而是没有独立网格。测试版现在以模型
  中心轴镜像左侧网格生成右耳鳍，左右共用原有物理结果，并继续支持对称旋转、间距和整体旋转。
- 暂停逐帧动态挂点差值修正，本轮使用固定校准变换验证显示、缩放与基础动作；诊断报告新增
  整体舞台变换、挂点模式和右耳鳍来源。

## v0.1.0（第一版测试版）

- 主模型：用户修改后的菜菜女仆；原服装、网格、物理和动作保留。
- Sen 动力层：当前使用完整编译 `.moc3`，只渲染呆毛、`Part113` 耳鳍和 `Part239`
  尾巴；其余 Sen 网格均不进入颜色绘制。
- `Part115` 兔耳结：默认排除；诊断报告单列其子网格，并报告选中配件的遮罩依赖。
- 私有包：单 ZIP + `accessory-lab.json`，App 只需一次导入。
- 贴图：主模型 4 张；Sen 只提供用户改色后的 slot 06/16/19。运行时按选中网格及遮罩依赖
  请求贴图，缺失依赖会提示并写进诊断清单。
- 图层：尾巴 → 主模型低层（含双马尾）→ 耳鳍 → 主模型高层 → 呆毛。
- 挂点：复制双方共有的头部、身体和呼吸参数；叠加主模型与配件模型的逐帧锚点位移差修正。
- 校准：三配件均有 X/Y/缩放/显示；耳鳍另有对称旋转、对称间距、成对中心轴旋转。
- 静止：暂停待机、测试运动、物理推进与点击反馈，作为位置诊断零点。
- 诊断：导出校准数值、配件网格、父 Part、贴图槽、遮罩源和 `Part115` 状态。
- 主模型交互：7 个互斥表情、7 个互斥动作；白袜/丝袜带子/双马尾/发带可独立切换；
  “变小”可与所有组叠加；点击预设仅在非静止状态触发。
- 袜子：默认黑袜，按钮初始显示“白袜”；切到白袜后按钮显示“黑袜”。
- 颜色：`ArtMesh122` 与 `ArtMesh149` 强制为 `#9E9EB2`；Sen 三配件使用用户已改色贴图，
  不叠加旧 Sen ArtMesh 色表。

## 后续验证

1. 真机依次运行头部摆动、身体摆动、变小和自动巡检，检查头部两配件与尾巴的跟随方向、
   旋转幅度和缩放比例。
2. 在挂件连接启用时再次触发耳鳍双抖，确认局部物理没有被刚性锚点抵消。
3. 测试奶茶、菜单、左右餐盘等大幅姿势，并观察尾巴是否仍稳定连接腰胯。
4. 若某一组出现持续偏差，再导出当前版本 JSON；静止位置无误时无需重新校准。
5. 用户提供原始 `.cmo3` 后，在 Cubism Editor 中删除无关 ArtMesh/Deformer/纹理并重新导出精简
   donor `.moc3`；保持文件名和 manifest 路径不变即可替换。
