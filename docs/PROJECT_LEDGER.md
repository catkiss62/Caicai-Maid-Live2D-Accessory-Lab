# 菜菜女仆配件实验室 · 项目总账

## v0.1.33（耳鳍侧转大小独立校正；待真机确认）

- 用户在 v0.1.32 观察到向一侧过脸时耳鳍本体缩小，并因此感觉发生位移。诊断保存的是两耳**整体外侧跨度**，未保存每只耳鳍的宽高；按 `head_turn_signed` 分组，正面跨度中位数约 1.215 clip，两侧大幅转脸均约 1.058 clip（收窄约 13%）。v0.1.31 放大倍率不同但相应收窄比率同样约 13%，可证实前次只调整 Y 位移未碰到这一现象。整体跨度不能单独证明某只耳鳍本体缩放。渲染代码仍把头部网格 `0.65` 的缩放响应直接用于每只耳鳍的绘制变换，是与用户观察相符的额外缩小来源。
- 在现有已经计算好的两侧**定位点**上，让耳鳍绘制使用单位缩放，仅保留头部旋转；两侧 X/Y 定位、透视间距上限、整对平移、用户校准及 Sen 原生网格动态仍走原路径。这将隔离脸部大三角的缩放，不改变既有“侧转两耳靠近”的约束。
- 左右逐帧诊断分别新增两只耳鳍的实际宽、高相对中立矩形的比值；同时区分 `fin_drawable_scale_response=0` 与 `pin_position_scale_response=0.65`。中立矩形来自捐赠模型的保存边界，经既有校准投影，仅用于跨左右帧对比；比值不是可见像素面积。
- v0.1.32 试验档确实计算出大幅左右侧转约 −4.7°/+7.5° 的局部转角校正，最终数学根点误差约 0；用户肉眼仍看不到改善。因此本版停止追加呆毛角度、拉伸或假位移，v0.1.30 默认档和 v0.1.32 表层试验档均保留。需要同姿态截图/视频对照可见发根与选中的网格接点，再决定是否换真正的视觉连接点。
- 构建状态与真机结果待记录。

## v0.1.32（局部发丝转角与耳鳍侧转竖向幅度；待真机确认）

- v0.1.31 真机反馈“呆毛没变化，耳鳍没变化”。诊断确认表层试验启用、选中 `ArtMesh386`（绘制顺序 284）、耳鳍局部竖向系数为 0.45。呆毛弹性后根点最大误差 `1.49e-8` clip，最终 `7.45e-9`：先前将可见错位归咎于弹性后根点脱钩的推断被证伪。v0.1.30 与 v0.1.31 的左右轨迹按放大倍率归一化后，根点 X 轨迹相近；仅更换前层位置点不足以让呆毛外形沿局部发片旋转。
- **只在表层试验档**，从女仆顶部头发固定三角计算局部转角与脸部整体转角之差，在已锁定的发根附近旋转 Sen 呆毛整组，最大校正 0.22 rad；随后保留原生弹性和最终根点锁定。默认 v0.1.30 呆毛档不受此项影响。诊断逐帧新增发片转角、实际校正角，用于判断这个假设是否产生足够大的效果；目前无真机视觉结论。
- 耳鳍此前仅将脸部网格相对刚性头部运动的竖向残差从 0.60 改成 0.45，但没有缩短刚性旋转本身产生的两侧反向升降。这版保留头部共同上下移动，以 0.55 比例保留两侧定位点相对头部中心的升降，继续保留原来的水平约束、Sen 原生双抖和尾巴。旧版与新版不同倍率下不能直接拿 clip 绝对 Y 作视觉幅度比较。
- 版本号 0.1.32；本地源码提交 `14c9c5878b0d8ace2f87b198065ad7fd2bbcda83` 与远端等价提交 `7bad1af7a09e4c000adbc24cb413016164858e92` 的文件树均为 `358a105d35309a0253104888d5412134896e112f`。Actions [36084293902](https://github.com/catkiss62/Caicai-Maid-Live2D-Accessory-Lab/actions/runs/36084293902) 成功，上传 v0.1.32 APK。产物包和 APK 的 ZIP CRC 检查通过，APK Manifest 包含 `0.1.32-local-hair-rotation`，3,876,803 bytes，SHA-256 `6200dce265de048f93d1e4fc1d3ba326cfa148d53d3c5987ac7ada3234c1f445`。构建成功不代表左右大幅真机视觉验收完成；下次诊断可对照逐帧局部发片与实际校正角。

## v0.1.31（v0.1.30 呆毛默认与表层发根试验；待真机确认）

- 用户指出左右过脸时呆毛**可见根部**仍明显错位。v0.1.30 诊断保存的选点是 `ArtMesh674`（绘制顺序 281）；同组 `ArtMesh386/387/388` 顺序 284/283/282，更靠前。旧选点器只取最近三角，重叠命中零距离时后遍历者可覆盖先前的表层命中。另一个疏漏是“根点误差≈0”在弹性逐顶点变形前读取；三顶点重心根点经不同权重变形后，不能仅凭此前误差认定画面已贴合。
- 新增“呆毛发根”对照按钮：默认始终使用 v0.1.30 保存的原选点及运动；试验按钮启用独立表层选点，用户先选中立并在可见发根点一次。试验选点优先包含点击位置的最前绘制网格，试验点独立保存，切回默认不覆盖旧点；重启仍回默认。试验模式在既有弹性后重新读根点，并通过仅作用于呆毛 projection 的整组平移锁住最终重心根点；不加侧推、改变原生形变或尾巴物理。
- 诊断分别给出默认/试验选点、当前 drawable 绘制顺序、弹性前后及最终锁定后误差，并给左右逐帧记录增加两组根点误差。结果仅表示几何选点，选点纹理透明度及肉眼覆盖关系仍需真机确认。
- 耳鳍保持水平约束、位置、图层和原生双抖；将侧脸表面给耳根的额外**竖向**补偿由 0.60 轻收至 0.45（四分之一），左右逐帧诊断新记两侧中心 Y，供观察过脸时是否仍偏远。此项适用于两档呆毛对照。
- 源码提交 `973ec16f593a003f8417d1b368bca3edf39e59d5`，Actions [36056764751](https://github.com/catkiss62/Caicai-Maid-Live2D-Accessory-Lab/actions/runs/36056764751) 构建成功；v0.1.31 APK 3,876,815 bytes，ZIP CRC/结构检查通过，SHA-256 `b42deabe2c5f7b31a487475abff57cafd60299c539c1a9d603f43ceb1b37e4e5`。在真机先选中立→切表层试验→点可见发根，再运行左右大幅比较根部及耳鳍；可立即切回默认呆毛。视觉效果未被构建验证。

## v0.1.30（恢复 Sen 原生尾巴物理输入；待真机确认）

- 纠正 v0.1.29 对“恢复原生尾巴”的误判：只恢复 `applyTailMirror()`，没有恢复驱动网格的输入；镜像方法本身仅取负 X，不会产生运动。Sen 原项目 `SenLive2DModel.update()` 每帧在物理求解前写入 `ParamBreath = performance.getBreathValue()`；菜菜配件分支 `updateCompositeOverlay()` 自 v0.1.18 最早可读源码起一直没有写入这一值。Sen 原始 `physics3.json` 的 `PhysicsSetting71` 只有 `ParamBreath` 一项输入（权重 66），输出 8 个尾巴变形器参数 `Param_Angle_TemporaryDeformer5..12`，`cdi3.json` 逐项标记为 `[0]tail..[7]tail`。原项目的代码和模型物理链一起说明尾巴如何运动；早期菜菜版本用户确认的动态不能由镜像方法单独证明。
- 配件分支现在沿用现有 `updateAccessoryEarOnly()` 推进的 Sen 呼吸时钟，在原生物理调用前写入原项目相同的 `ParamBreath` 值。保留女仆身体三角挂点、尾巴原生网格/镜像、呆毛及耳鳍设置，不再使用逐顶点人工摇摆。静止模式仍不推进呼吸或物理。
- 源码与物理文件的输入/输出及调用先后已核查；构建通过后仍需真机观察尾巴实际摆动，不能把构建成功当作画面验收。
- 远端源码提交 `35ca2558bca55dd3af540862077fe8a54ffeaa73`，构建文件名修正提交 `6678154407e5840a199bdcd249547f972028727c`。Actions [36049887379](https://github.com/catkiss62/Caicai-Maid-Live2D-Accessory-Lab/actions/runs/36049887379) 成功并上传 v0.1.30 APK；APK ZIP 结构和 CRC 检验通过，文件 3,876,803 bytes，SHA-256 `4c6f8d5fe38096c45c5cf0abe8cb3c0112769f306da4a6bcec6b8d375df71868`。真实画面尚待真机确认。

## v0.1.29（撤销额外侧推和人工尾巴摆动；待真机确认）

- 用户反馈 v0.1.28 呆毛左右大幅拉伸比上版明显。代码对照显示 v0.1.27→v0.1.28 没有改绑定算法；更改的是新安装/重置默认外形，旧手调存档应沿用，尚无 v0.1.28 左右逐帧诊断可确定真实姿态差异。v0.1.26 起存在“左右侧转 × 当前呆毛可见宽度 × 15%”的逐顶点外段侧推；高度 1.59、旋转 -14° 的外形使外段更明显。撤掉持续侧推，返回 v0.1.25 已验证宽窄收敛的根点固定+速度弹性路径；手调外形及位置值仍保留，左右跟随程度需真机复核。诊断字段中的目标侧移改为 0，保留弹性前后宽度记录。
- 尾巴：用户指出旧版曾正常，上一轮没有先比对就加了尾端正弦摆动，属于不当改动。v0.1.16 APK 可确认有 `applyTailMirror` 方法；仓库最早可读源码 v0.1.18 与 v0.1.25/v0.1.27 均沿用 Sen 原生物理网格与这一路径。撤销新增逐顶点摆动和对应报告字段，恢复旧源码路径。若真机仍不动，应先依据具体动作与帧数据区分原生动态和身体载体运动，不再盲目补动画。
- 进入“变小”时仍默认启用 `2插手`、`1生气`；退出“变小”时同时关闭这两项，不保留小形态表情。耳鳍位置、图层、原蒙版以及整体调整保持已确认状态。
- 本地修改提交 `e1d76e2` 与远端提交 `61e6deeb5aa6ce010dafb5ed532846ad1ba49239` 文件树一致。Actions [36042863365](https://github.com/catkiss62/Caicai-Maid-Live2D-Accessory-Lab/actions/runs/36042863365) 成功并上传 v0.1.29 APK；包内 CRC/结构检查通过，SHA-256 `08c3af9ee2060248b50cd5bbb1584ce4c0d3833f92a57ab6cec9ea829bab5e64`。呆毛大幅侧转、尾巴及退出变小的画面仍需真机确认。


## v0.1.28（呆毛外形默认、尾巴摆动、怒脸首帧、变小初始姿态；待真机确认）

- v0.1.27 的真机报告确认呆毛外形宽 1.15、高 1.59、绕根点 -14°，位置缩放 1.00、X +0.01、Y -0.05；将这些设为新安装与“重置当前”的默认。已保存的用户值继续优先，不修改呆毛横向绑定；下一轮再根据真机观察处理左右大幅跟随。
- 耳鳍仍使用报告中已确认的整对及两侧位置和图层。用户测试耳鳍旧兔耳蒙版绕过无效，自行修 PNG；移除对照开关、报告字段、渲染补丁与构建时的补丁应用，恢复原生蒙版。
- 尾巴在固定中立姿态的 Sen 配件层缺少持续侧向输入：从原生物理求出的尾巴网格出发，仅让下端以最多 0.12 模型单位缓慢左右摆动，上端权重为零，完全静止时偏移归零；在位置报告中输出当前末端偏移。此为恢复摆动的受控试验，幅度和观感待真机确认。
- `1生气.exp3.json` 只设定 `Param148`，原 VTS 淡入 0.5 秒。使这一脸部切换直接进入目标关键形，并在开始半秒把嘴部开合保持关闭，避免中间张嘴帧。开启“变小”时自动选中原生 `2插手` 与 `1生气`，之后仍可各自切换；关闭“变小”不自动移除这两项。
- 对应本地提交 `dab42f5`；经 GitHub 接口重建并比对相同文件树 `99c8a572` 后，远端提交为 `e3aa02142df4049647ad64c840f916219fbc3ca0`。Actions [36041224073](https://github.com/catkiss62/Caicai-Maid-Live2D-Accessory-Lab/actions/runs/36041224073) 完成并成功上传 v0.1.28 APK。APK ZIP 结构与 CRC 已检查，SHA-256 `2e038e9eef6b1a8a2487cc969b723e49a5f4f3f8520c74fc5e55b1700ce299bb`；尾巴摆动、怒脸首帧和变小初始姿态待真机确认。


## v0.1.27（耳鳍旧兔耳蒙版局部绕过、手调默认与呆毛外形；待真机确认）

- 用户确认上一份文件实际是 v0.1.25 的诊断，不能据此判断 v0.1.26 呆毛绑定。只采用其中已手工调好的耳鳍设置作新安装及重置默认：整对缩放 1.24、X -0.01、Y -0.33、整体旋转 +5°；画面左侧 X +0.05、旋转 -9°、图层 -3；画面右侧 X -0.04、Y +0.02、旋转 +14°、图层 0。已有应用保存的数值仍按原值读取。
- Sen 的 `texture_16.png` 留有鱼鳍纹理；原兔耳外侧两个可见网格 `ArtMesh629`、`ArtMesh723` 仍受原兔耳蒙版约束。仅在绘制这两个耳鳍网格时绕过原蒙版，不改其网格、纹理、头部跟随、图层，也不影响其他网格；应用内提供默认开启的“显示完整 / 原兔耳蒙版”切换，并在诊断标记开关状态及目标网格。若素材本身网格或纹理已有裁切，绕过蒙版也可能无法补出缺失区域，需同姿态真机对照确认。
- 呆毛增加独立宽度、高度（每步 0.03）及根点旋转（每步 2°）按钮，在 Sen 六网格的现有固定根点局部变换阶段施加；保留现有弹性及与头发连接点的运动绑定。重置呆毛可恢复三项外形参数。下一轮等用户确认静止外形后再研究绑定。
- 源码 `43091cb03b17f5db0be6b0291fd17bdb73db2513` 已推送 `main`；Actions `36007220215` 的渲染器补丁与 APK 构建均成功。APK 已检查压缩完整性，SHA-256 `b20fd566879bafcfc575cbb9b8ba7d6e9de9feeac41897221a3ad7f9e132791c`。蒙版改变后的可见外缘仍需同一头部角度下真机比较；静止外形调好后再反馈呆毛随动。


## v0.1.26（保持根点锁定，增加呆毛远端随侧脸移动；待真机确认）

- 用户确认 v0.1.25 呆毛已不再错误变宽变窄、弹性正常；歪头正常，上下只有轻微偏移，但左右大幅时呆毛整体显得跟不上远处头发。该版“左右大幅”报告共 573 帧、约 12 秒：用户手选的 `ArtMesh674` 属于允许的顶部头发网格，根点到该点的误差为 0、最大误差约 `2.4e-7 clip`。侧转正负极限根点 X 中位数分别约 `-0.084/-0.251 clip`，确实有跟随位移；呆毛整体缩放恒为 1，弹性前可见宽度分别约 `0.474/0.472 clip`，先前的宽窄问题已明显收敛。
- 本版只在根点以外的 Sen 六段呆毛上，按女仆水平头转方向叠加一个以呆毛当前实际宽度 15% 为满幅的连续侧向跟随。沿用当前逐顶点连续权重：选定根点仍为零位移，末端随头更远，中段平顺衔接；原有速度弹性和总缩放设置不改。反向时符号随 `ParamAngleX3` 连续翻转，静止或手势重置时侧转姿态仍正确生效。耳鳍和整体缩放/拖动功能保持已真机通过版本。
- 逐帧诊断新增侧转姿态的目标 X 位移及最终呆毛实际 X 中心，可直接对照根点运动与远端总运动。真机重点看左右最远两端连接位置与远端覆盖，是否仍跟不上或出现过冲；再复测歪头及轻微上下偏移。
- 源码提交 `e1fb17a31e3a7a846b0ef110cf00e0e1e24774cc` 已推送 `main`；Actions 运行 `35989989431` 构建成功，APK 压缩完整性检查通过，SHA-256 `ce07678df519801d33c8c86590ed6240be44f3cd5443c2e4b5d02fb43fdff67f`。呆毛左右最远位置与连接连续性仍需真机确认。

## v0.1.25（呆毛侧转宽窄与惯性分离；待真机确认）

- 用户确认 v0.1.24 耳鳍效果正常，耳鳍位置、双抖及图层保持原样。该版约 19 秒、900 帧的呆毛报告显示：水平头转正负极限附近，即使接近停住，可见宽度中位数仍约为 `0.329/0.212 clip`；根部至用户在女仆 `ArtMesh386` 选定点的修正后误差为零。
- 根点绑定路径原先仍使用女仆**脸部**三角的侧转缩放：左右极限分别约 `1.056/0.857` 倍。另在呆毛局部弹簧里持续加入 `-headAngle*.035` 横移与 `-headAngle*.16` 旋转；这些在停到侧脸时仍生效，根点固定无法阻止远端轮廓变宽或变窄。本版仅让呆毛继承脸部角度、将整段倍率固定为 `1.0`，根点仍逐帧贴到选定头发点；局部弹簧只读取根点和头转**速度**，停止后自然回正。Sen 原生六网格、方向点、随动弹性和整体调整均保留。
- 左右大幅逐帧诊断继续保存最终轮廓宽度，另加施加弹性前的轮廓宽度与本帧弯曲角，以分辨下一轮剩余的原生形变和局部弹性贡献。真机先对比两侧停住后的轮廓与中立宽度，再看运动时是否仍有自然弹性；如果仍不对，不要将构建成功当作视觉通过。
- 源码 `7ae585dcf06adfe0e1f82dfb6296f899c0b70752` 已推送 `main`；Actions 运行 `35985480973` 构建成功，产物 APK 已解包检查完整，SHA-256 `3695e5c5945f97efc5766d646d282dfa5b829f5ec3b3f4edde81146b9318159a`。耳鳍已由用户在 v0.1.24 确认；本版呆毛变化仍待真机确认。

## v0.1.24（诊断证实过正面耳鳍跳变，保持侧脸位置连续修正；待真机确认）

- 用户发来 v0.1.23 “实际网格校正”+“左右大幅”报告，共 585 帧、约 12.1 秒。`sen_native_ear_drive` 全程为 0；脸部视差与 `ParamAngleX3` 的符号在全部 585 帧相反，超过旧阈值的 537 帧均选择 `-head_turn_signed`。剩余 48 帧近正面切回 `+head_turn_signed`，形成 13 次左右耳同向位置突跳，双耳中心最大单帧变化约 `0.0255 clip`；数值上已捕获用户所见颤动。外缘间距限制连续，颤动发生在同向位移方向切换。
- 将耳鳍同向位移方向改为连续的 `-ParamAngleX3` 归一化值，消除视差阈值分支。依据真机这段数据，它在侧转时与 v0.1.21 的正确方向完全一致，只改变正面附近约 `|head_turn| ≤ 0.131` 的错误反向区间；幅度仍为中立跨度 7%，左右独立头部绑定、外缘约束及 Sen 原生双抖保持。
- 用户补充 v0.1.21 时代呆毛可能也已随左右过头变宽、变窄。现有报告只显示已选 `ArtMesh386` 根点后误差为 0、最大误差约 `2.4e-7 clip`，并不包含下半段宽度的逐帧信息，不能据此断言宽窄由哪一层造成。本版保留 v0.1.21 的整组六网格和手选根点，新增呆毛逐帧实际轮廓宽度、方向相对根点坐标和头部整体缩放/角度；下一次同样做“左右大幅”两三轮直接导出 JSON，可检查左右极限轮廓的真实差异及来源。
- 源码 `534ae9575c343af6d8e2fbcd83398d027846ecbc` 已推送 `main`；Actions `35943046852` 编译成功，`Caicai-Maid-Live2D-Accessory-Lab-v0.1.24.apk` 已检查压缩完整性，SHA-256 `95ee1fc341fc15604c0d135e6a68f42aac3f2a6ceac481b01d7a8fa784ea115a`。侧脸位置与呆毛宽窄变化仍须以新 APK 真机画面及其报告验证。

## v0.1.23（还原已确认位置并记录左右扫头逐帧数据；待真机确认）

- 用户更正 v0.1.22 真机判断：新加入的呆毛头发姿态修正使向右侧过头时呆毛错误变宽、向左时变窄，整体外观不对；耳鳍也从 v0.1.21 的正确距离退化为右转太靠右、左转太靠左。撤销 v0.1.22 的呆毛相对头发三角缩放和旋转，恢复 v0.1.21 手选根点及原生连续网格；耳鳍恢复 v0.1.21 的脸部视差优先方向规则、7% 同向位移及既有外缘约束。已确认正确的 Sen 整体调整不变。呆毛最远距离仍待定，避免在错位方向上放大。
- 原报告只保存导出瞬间，无法定位过正面的短促颤动。本版在“实际网格校正”与“左右大幅”同时开启时保存最近 900 个有效绘制帧的环形记录，每帧记录单调时间、头部有符号角度、脸部视差、选用的耳鳍方向、同向位移、左右耳鳍实际中心 X、实际外缘间距和 Sen 原生耳抖驱动。重新点击“左右大幅”会清空上一段，导出报告前保持动作运行；如果帧数超出 900，自动保留最后 900 帧。
- 待用户真机：点“左右大幅”保持“实际网格校正：开启”，运行约两三轮后直接导出 `v0.1.23` JSON 并发送；如果能录到颤抖画面，附短视频更便于对齐帧时间。需要确认恢复的位置和下半段轮廓，再根据逐帧曲线分别处理耳鳍短促颤动与呆毛最远距离。
- 源码提交 `4b5bab475a90f7739107086b245ea9db404ce502` 已推送 `main`；Actions `35941253396` 编译成功，APK 下载解包及压缩完整性检查通过。`Caicai-Maid-Live2D-Accessory-Lab-v0.1.23.apk` SHA-256：`bf73f1c9c8c90a8806a57b4224cfef7a7e8cc10e96b3f3facdfdd2c1d814bf59`。新一轮耳鳍颤抖与呆毛外观仍待真机数据。

## v0.1.22（耳鳍过中立颤动的连续方向修正；待真机确认）

- 用户真机确认 v0.1.21 整体调整完全修好，耳鳍在各个头部方向正确跟随；保留两项成果。左右扫过正面时耳鳍会突然颤一下，呆毛歪头大幅稳定、上下略偏、左右偏得更多。
- 本次附的报告仍是 v0.1.20、中立动作、旧版对照模式，不能证实 v0.1.21 某一帧的颤动。检查代码发现耳鳍同向偏移用脸部视差超过 0.3% 中立跨度为阈值来切换符号，若视差在非零头转角下过零，两耳会同时跳。取消阈值切换，持续用有符号 `ParamAngleX3` 控制整对耳鳍的方向；先前侧转报告显示该参数与视差方向一致。保持已真机确认的 7% 测试幅度、外缘约束与原生双抖。
- 用户补充允许尝试呆毛的立体跟随。手选点与顶部头发运动三角均在女仆 `ArtMesh386`，选点继续锁定 Sen 根部。读取这块头发相对原有头部刚性路径的缩放和角度变化，以 65% 响应并限制在 `0.78–1.22` 倍及 `±0.18` 弧度，对 Sen 呆毛六网格围绕已选根点作同一次变换；随后再核对根点误差，不逐顶点覆盖或挪动下半段。此为立体动作试验，不能把数学上根点零误差等同于肉眼连接效果，诊断新增本帧形状修正倍率/角度及峰值角度。
- 旧报告导出时对照开关处于 `v0.1.16_baseline`，不能据此判断用户本次看的模式。新版待真机观察：开“实际网格校正”做左右大幅，看过正面时两耳是否还同时跳；分别看呆毛连接处和下段轮廓，再对比上下大幅与歪头大幅。
- 最终源码 `38d62a83e5191e2c42ee5c78ac684d7ba60cc054` 已推送 `main`；Actions `35918805403` 编译成功，产物 `Caicai-Maid-Live2D-Accessory-Lab-v0.1.22.apk`，SHA-256 `9d6829902d74b55f4db0ec7efe2e739a3cfa80d9a6c268aee23edcd870b48799`，APK 压缩检查通过。尚未在设备上确认颤动是否消失或呆毛外观是否改善。

## v0.1.21（耳鳍方向及 Sen 整体调整方法；待真机确认）

- 用户在 v0.1.20 真机确认手选呆毛连接点成功。诊断中 `ArtMesh386` 已保存三顶点 `[130,121,120]` 与三项重心权重，最大新版根点误差约 `1.2e-7` clip；保持选点与呆毛运动路径不变。导出时模式为中立且 v0.1.16 对照，不能拿导出帧的 0 位移证明侧转方向。
- 用户真机指出耳鳍向右侧过头时反而更向右。保留现阶段用于检验有效性的 7% 中立跨度测试幅度，只反转整对耳鳍的同向位移符号；继续维持原外缘间距限制。新增左右实际可见耳鳍修正前后中心 X 和整对平均实际屏幕位移，下一次可直接核对符号。
- 逐段对比 Sen 测试仓已正常使用的整体调整：菜菜版漏掉以双指焦点补偿缩放位移、双指结束后接管的单指起点、UI 侧位移限幅；并额外用了逐帧可能变化的人物中心作为缩放支点。现按 Sen 的焦点公式与手势状态更新，两个模型共用固定屏幕原点的最终舞台缩放及位移，避免主模型与配件不同步。已保存的静止配件校准与手选根点保留。
- 本轮需真机确认：耳鳍右转向左、左转向右；缩放时手指焦点下的内容稳定；双指抬起后继续单指拖动；重新放大缩小后手选呆毛仍跟随头发。
- 最终源码 `19c6403` 已推送 `main`；Actions `35915695993` 编译与 APK 上传成功。`Caicai-Maid-Live2D-Accessory-Lab-v0.1.21.apk` SHA-256 为 `f836515c68538a711479702bfb03d1f23e4cf75bafd822de0a9ef0619759e12a`。APK 压缩包完整；实际画面修正方向和手势体感仍待真机确认。

## v0.1.20（手选头发连接点与可见耳鳍位移；待真机确认）

- 上版真机反馈：呆毛完全不动，耳鳍修正仍不可见。核对 Sen 总账和源码后确认，Sen 两个手选点都在其自身呆毛网格上，原生 Hair Z 才负责驱动；菜菜配件模式不传 Sen 头部物理。v0.1.19 将 Sen 第二点强制对齐菜菜头部方向，会抵消摆动，不能作为跨模型固定方法。
- 新增一次性点选：中立姿势点菜菜顶部头发的真实连接处，程序在该头发网格的可见三角形上记录 drawable ID、三顶点 ID 与重心权重。命中后整组六网格立即跟到点上，供用户用呆毛本身确认位置；未命中时提示重新点。重新导入其他模型时清除旧点。
- 保留 v0.1.16 已真机确认的头部脸部运动路径，随后统一平移六个 Sen 呆毛网格，让确认根点逐帧落在手选头发点。Sen 方向点仅用于自身局部外观，不再与菜菜头部第二点硬对齐；菜菜头转与速度产生逐渐增强的弹性弯曲，根点权重零，无僵硬的整段根区。不选点前显示 v0.1.16 路径，方便安全校准。
- 耳鳍同向移动满幅由中立双耳宽的 1.8% 提高到 7%，先检查改动是否实际可见，后续依据截图回调；原有外缘间距上限、双耳原生抖动、图层 -1 默认、变小路径仍保留。
- 导出诊断记录手选 drawable 与权重、根点实际修正前后差距及双耳实际位移。构建通过只能证明代码可运行，连接点与摆动视觉正确性必须由真机确认。
- 最终源码 `da710cc` 已推送 `main`；Actions `35905938506` 编译和 APK 上传成功。测试包 `Caicai-Maid-Live2D-Accessory-Lab-v0.1.20.apk`，SHA-256 `904e607d7127d96bedeee1901a135d850655160cb66941dfaeaa44823587fc3a`。等待用户在中立状态手选连接点、左右大幅观察根部与弯曲以及耳鳍同向移动，再决定是否收小测试幅度。

## v0.1.19（Sen 双点固定与耳鳍随侧脸轻移；待真机验证）

- 用户真机确认 v0.1.18 的耳鳍有明显进展，但呆毛近根网格校正使下半段僵住且外观被截断。
  截图中向画面右侧过脸时，两只耳鳍都偏右：右耳过远、左耳过近；应整体略向左，反向过脸
  整体略向右。诊断记录实际网格校正曾命中，但根点零误差并不能证明下段轮廓连续。
- 呆毛撤销六网格近根部逐顶点绑定形状恢复；沿用 Sen 已由用户手工选出的根点与朝上方向点
  形成二维局部框架，将两点映射至女仆顶部头发的根部和随头部转动的方向，整组六网格只做
  一次一致变换。原生下段形变与末端二次摆动保留，不搬用 Sen 项目的缩放旋转数值。
- 耳鳍保留 v0.1.18 的实际外缘间距上限，随后两只耳鳍同向轻移，满幅侧脸最大量为中立双耳
  跨度的 1.8%；优先用脸部固定三角相对头部整体运动的横向漂移判断画面侧向，过小则使用
  `ParamAngleX3` 的有符号头转。诊断新增 `ear_shared_shift_clip`、`ear_face_parallax_clip`、
  `ear_screen_turn_signed`、`direction_to_head_axis_after_clip`，真机可直接判断方向和幅度。
- 左右耳鳍图层默认偏移均设为 `-1`（用户导出校准确认），重置到该值；已有保存值继续使用。
  下一轮真机优先测试左右大幅的同向移动与呆毛下段连续性，再检查中立、歪头及变小。
- 源码提交 `9ed39cf` 已推送 `main`；Actions `35898439142` 编译及 APK 上传成功。
  测试包 `Caicai-Maid-Live2D-Accessory-Lab-v0.1.19.apk`，SHA-256
  `d221ae1a33f7daede1a784fad67c39ed2d6a977a855fc466d6ba60b9243d8b0b`。编译通过，
  侧脸方向与呆毛连续性仍等待真机观察。

## v0.1.18（实际网格约束与 v0.1.16 同屏切换；待真机验证）

- 接受 v0.1.17 真机“没有任何改变”的结论，从已确认的 v0.1.16 做对照。在同一画面点
  “实际网格校正：开启 / v0.1.16 原版：开启”即可切换，不需要卸载、改存档或反复导入素材。
- 新档仍保留已确认的缩小动作、静止校准、双耳独立图层及尾巴路径。左右耳鳍绘制前读取两套
  最终投影下实际可见网格的外缘，按中立间距及当前 `ParamAngleX3` 水平头转量设置收窄上限，
  超出时将两只完整耳鳍等量向头部移动；原生双抖不改。旧档使用 v0.1.16 的独立脸部三点跟随。
- 呆毛新档以女仆顶部头发的固定网格点为运动目标，保持原静止偏移；六个网格靠根部 12% 的
  顶点逐帧恢复绑定形状，至 45% 平滑过渡到原生形变，末端及二次摆动保留。旧档使用
  v0.1.16 的脸部顶部定位点与原生呆毛几何。
- 导出诊断 `geometry_comparison` 包含两档运行帧数、实际耳鳍外缘修正前/上限/修正后间距、
  每侧水平修正量、归一化头转量、呆毛根部到头发目标的修正前后误差、近根顶点改变量和缺失
  锚点计数。若真机仍无变化，先查是否记录到了非零修正与修正后的实际网格位置。
- 远端源码提交 `3f03064`；Actions `35888888326` 编译及 APK 上传成功。产物
  `Caicai-Maid-Live2D-Accessory-Lab-v0.1.18.apk`，SHA-256
  `4a1bef0ae2c47fca4d2b84ce2884031c8c0e21abadfbb24dd3b97a67012f1aba`。
  构建通过不代表视觉效果已验证。真机先选“左右大幅”切两档比较，再看歪头大摆、变小和尾巴。

## v0.1.17（呆毛根锁与耳鳍间距约束；真机无可见改善）

- 以用户认定效果最好的 v0.1.16 为基线。该版的变小动作已确认正常；图层、静止校准、尾巴
  和缩放路径保持不变。v0.1.17 只处理头部左右大幅转动时的耳鳍间距，以及呆毛根部轻微偏移。
- 两只耳鳍各自读取独立的脸部网格锚点，减弱侧脸局部横向拉伸，并将过宽的双耳间距限制在
  稳定头部间距的 103%；透视造成的间距收窄仍允许，避免把双耳硬做成一体。
- 呆毛以 Sen `ArtMesh151` 确认的根部锚点逐帧跟随女仆头部，抵消来源模型自身造成的根部漂移。
  根部附近 10% 的六网格区域不给二次物理权重，之后平滑增大至末端；头部速度驱动有界弹簧，
  舞台缩放与拖动不作为惯性输入。这是运行时网格修正，最终视觉效果仍需真机检查。
- 远端 `main` 提交 `ab8112a`；Actions `35885269890` 构建成功，产物
  `Caicai-Maid-Live2D-Accessory-Lab-v0.1.17.apk`。用户真机反馈：相对 v0.1.16 没有任何
  可见改变，因此不得将这一轮标为修复成功；v0.1.16 仍是已确认的基线。
- 代码复核发现策略的两个盲点：耳鳍上限是相对“由脸部大三角推算的整体头部间距”，该参考值
  自身可以变宽，而且约束的是两个定位点间距，并非最终绘制出的耳鳍根部或外缘；呆毛零权重
  只关闭新增弹簧对根部附近的影响，未锁住 Sen 原有的邻近顶点形变，目标点也取自脸部定位点
  加中立偏移，而非画面上实际的头发连接位置。是否还有运行时路径未生效，需要下一版诊断确认。
- 下一步以可见网格为测量对象：在最终绘制坐标测量双耳实际根部及外缘，约束左右各自的根部
  与头部轮廓关系；呆毛选择女仆顶部头发的固定重心坐标为目标，校正根部及连续几个近根截面，
  保留远端原生摆动。新诊断记录每帧原始/目标/最终锚点和约束命中量，避免再次只改代理量。

## v0.1.16（脸部三点独立绑定；已真机反馈）

- 呆毛和两只耳鳍分别固定到女仆原始脸部大网格的三个点；运动载体与已确认的绘制图层分开，
  改图层不会暗中改运动来源。耳鳍缩放响应已减弱，保留 Sen 原生双抖；尾巴路径未改。
- 用户反馈这是迄今效果最好的一版：歪头大摆恢复正常，变小后的耳鳍大小也正常。
  遗留问题是左右大幅转动时耳鳍间距过宽，呆毛有少量偏移，程度接近第一次成功绑定时。
- 远端提交 `a88b48f`；Actions `35877178346` 构建成功。后续绑定优化从本版出发，
  不回退到 v0.1.15 及更早的头部载体试验。

## v0.1.15（绘制层/载体解耦 + 头发素材节拆分）

- v0.1.14 真机与诊断确认失败原因。呆毛偏移 `-1` 后位于 `render order 627`，实际上在头饰
  `628–633` 后方；用户要求的是头饰画面前方一层，因此新默认阈值改为头饰最大 order `633`。
  旧逻辑还把载体换成 `Part16=头发阴影` 的 `ArtMesh99`；正确载体则是
  `ArtMesh363_Skinning/ArtMesh264_Skinning=顶部头发`。
- 呆毛的绘制插层和运动载体现已完全解耦：默认插层固定为头饰全部网格之后（画面前方一层）；无论用户怎样测试
  前后图层，根部都只读取顶部头发的固定三角。中立帧投影和已确认的缩放/X/Y 均不改变。
- v0.1.14 的“后发”递归组错误包含 `render order 0–371` 的三百多个网格，实际把左/右双马尾、
  多段后发和颜色分支全部吞成整体。该大组以及同类“前发”大组不再用于图层校准。
- 新候选层改用作者的蒙皮素材节 Part：后发左/右各段、左右双马尾与发丝、后发中间、顶部
  头发、编号头发层、侧发左右、散发等分别成为一步；同一素材节在颜色/样式分支中的 Part
  才合并。这里的 `texture_00–03.png` 是打包图集，不等于原始 PNG 图层，因此用蒙皮 Part
  比按整张 texture index 切分更接近原素材结构。
- 左右耳鳍继续独立选择层；非零偏移使用当前插层前侧素材节的对应屏幕侧固定三角。偏移零仍
  保留 v0.1.13 已验证的左右蝴蝶结载体，尾巴完全不改。
- 校准存储升级为 v3：呆毛新 `0` 直接表示头饰画面前方一层，不继承旧报告的 `-1`；
  全局、呆毛、耳鳍和尾巴的已确认默认缩放/X/Y数值保持原值。
- 诊断的每个素材节新增直属父 Part、render order、texture index 与 UV bounds，下一轮可直接
  锁定具体素材节，而不再依据递归大组猜测。

## v0.1.14（按 Part 粗粒度图层校准）

- 本轮只解决用户提出的“先由真机判断正确头发层，再决定最终载体”：不猜最终图层，不改
  v0.1.13 已确认的呆毛/双耳静止缩放和 X/Y，也不改已经通过的尾巴绑定。
- 新增明确方向的“往后（更容易被遮挡）/往前（更少遮挡）”按钮。呆毛单独保存一个图层偏移；
  耳鳍继续支持整对、画面左、画面右三个调节目标，左右可保存不同的图层偏移和数值反馈。
- 女仆头部只提供粗粒度逻辑 Part：后发、双马尾与后侧发、头顶发、脸、前发、头发阴影、
  两侧蝴蝶结、头饰。同一类金/粉/白/棕等颜色变体合并，不把单个 ArtMesh 暴露为一层。
- 主模型从固定“低层/高层”两段绘制改为按当前 render order 的多段绘制；配件插入所选 Part
  边界，女仆自身网格顺序不重排。偏移为 0 时，耳鳍仍在两侧蝴蝶结之前，呆毛仍在最前，
  因而默认画面与 v0.1.13 完全一致。
- 非零偏移用于同时预览载体是否吻合：呆毛读取当前插层后侧相邻 Part 的固定三角；每只耳鳍
  读取前侧相邻 Part 的对应屏幕侧固定三角。零偏移保留 v0.1.13 的头饰/左右蝴蝶结三角，
  防止首次启动位置变化。所有载体只读取几何，不继承 Part 透明度。
- “重置当前”同步把相应图层偏移归零；尾巴点击图层按钮只提示固定最后层，不写入无效数据。
- 位置诊断新增 `maid_part_layer_calibration`：完整记录逻辑 Part、合并后的 drawable 清单、
  前后插层、请求/实际槽位、render-order 阈值、相邻载体名称和实际固定三角，供下一轮固化。

## v0.1.13（蝴蝶结双侧透视挂件）

- v0.1.12 真机确认呆毛、耳鳍和尾巴的基础单向绑定成立：上下大幅更准确，“变小”三组均能
  同步缩放。剩余错误集中在左右侧脸：整张脸的统一二维相似变换会把两只耳鳍同向平移，不能
  表示一侧向镜头展开、另一侧向头部内收的拟三维透视。
- 女仆 `cdi3` 已确认存在 `Part30=蝴蝶结右`、`Part31=蝴蝶结左`。本版分别从两个 Part 选择
  固定局部三角，再依据中立帧 X 坐标分配为“画面左/画面右”，不直接相信编辑器左右命名。
- Sen 原生耳鳍九网格按中立模型 X 的最大间隔拆为两个过滤绘制 pass；没有复制或镜像网格，
  Sen 模型仍每帧只更新一次，`ParamL_angle/ParamR_angle/ParamR_angle2` 双抖链保持不变。
- 两侧耳鳍分别接收对应蝴蝶结局部三角的中立→当前变换；仅使用几何运动，不读取头饰 Part
  透明度。因此关闭女仆头饰不会隐藏耳鳍，耳鳍的显示/隐藏仍由独立 Sen 配件开关负责。
- 拆分后的两侧默认微调均为恒等值 `1.00/0.00/0.00/0°`，共同继承 v0.1.12 已确认的整对
  `1.24/-0.01/-0.29/整体+5°`，保证静止默认位置不因拆分而改变。
- 配件轮换仍严格只有“尾巴→呆毛→耳鳍”。耳鳍区新增独立调节目标“整对/画面左/画面右”，
  原缩放和 X/Y 按钮以及旋转按钮作用于当前目标，并显示、保存各自数值。
- 主模型分层阈值从“双马尾之后”改为“两侧蝴蝶结之前”：先画后侧主体，再画两只耳鳍，最后
  绘制蝴蝶结及更前层的头饰/头发，使蝴蝶结能够遮住耳鳍。图层关系不与头饰显隐绑定。
- 呆毛由整脸三角改为 `Part9=头饰` 的头顶局部三角，减少左右侧脸时的小幅中心漂移；已确认
  的尾巴身体载体完全不改。
- 诊断新增左右耳鳍实际 ArtMesh 清单、左右蝴蝶结固定顶点、独立微调值、图层策略及
  `ear_visibility_source=sen_accessory_only_not_headwear_opacity`。

## v0.1.12（女仆网格单向载体绑定）

- v0.1.11 真机结果确认未完成真正绑定：女仆左右大幅测试几乎不动，但呆毛/耳鳍大幅移动；
  歪头时可能反向也可能明显滞后；“变小”时耳鳍停在原地。由此确认 Sen 供体仍在接收不兼容
  的同名头身参数，之后的三角抵消无法可靠消除其骨架响应。
- 本版彻底删除女仆→Sen 的刚性头部、身体、手臂与衣摆参数复制。Sen 每帧从自身中立基线开始，
  只计算耳鳍双抖等配件局部动力；供体的头身骨架不再参与三个配件根节点运动。
- 呆毛和耳鳍读取女仆脸部固定三角、尾巴读取女仆裙身固定三角。三角从中立帧到当前帧的实际
  平移/旋转/缩放直接追加到各自已经校准好的配件 projection，是严格的女仆网格→配件单向
  载体，不再读取或抵消 Sen 脸/身体三角的当前运动。
- 临时左右测试改写女仆可见的 `ParamAngleX3`，上下测试使用 `ParamAngleY2`；完整动作参数表
  不在本轮猜测，后续由用户按肉眼结果逐项标注，用于自主待机。本轮唯一验收重点是配件根部
  是否随女仆实际网格同步移动及缩放。
- 诊断固定输出 `attachment_mode=maid_mesh_one_way_triangle_carriers`、
  `sen_rigid_parameter_drive=false` 与 `sen_local_accessory_dynamics=true`，便于真机报告确认没有
  回退到旧参数驱动路径。
- 保留三组独立筛选、原生双耳一次绘制、用户确认的零位与尾巴放大结果；没有恢复已删除的
  “呆毛+耳鳍”组合，也没有引入 Ruby。

## v0.1.11（正确模型基线 + 固定三角载体绑定）

- 当前唯一主模型是用户修改贴图的菜菜女仆原生 `maid.moc3`；Sen 仅作为三个配件的动力供体。
  当前源码不再加载、引用或以 Ruby 为绑定参考。v0.1.10 及以前关于 Ruby 的记录仅保留为失败
  试验史，禁止继续复用其判断和绑定路径。
- Sen 配件逻辑拆分保持干净：呆毛仅 6 个确认网格；耳鳍是 `Part113` 原生双耳参数链且只绘制
  一次；尾巴仅保留 `Part239` 中当前活动的可见网格。不存在“呆毛+耳鳍”组合筛选组。
- 女仆 `Part25=脸` 与 Sen `Part39=脸部` 分别选定一个固定 drawable 三角形，作为呆毛和耳鳍
  的共同头部载体；两组仍独立校准、独立隐藏、独立绘制。女仆 `Part29=裙子` 与 Sen
  `Part93=下装` 同样建立尾巴身体载体。
- 每帧先计算女仆载体从中立到当前帧的最小二乘相似变换，再抵消 Sen 载体自己的跨模型响应；
  三个配件只收到统一的最终 clip-space 修正，因此局部呆毛弯曲、双耳抖动和尾巴摆动不被抹掉。
- 载体使用固定 drawable 与固定三个顶点 ID，不再用会随动作改变极值的包围盒，也不再用只能
  表示位移的单点。诊断新增双方实际载体的 drawable、顶点 ID 和中立三角坐标。
- 保留最新确认零位：全局 `1.00/0.00/+0.20`；呆毛 `0.88/-0.03/-0.05`；耳鳍
  `1.24/-0.01/-0.29/整体+5°`；尾巴 `1.00/+0.02/-0.24`（已包含两次放大）。
- 新素材包统一为 `accessory-lab.json + caicai + sen-accessory`；Sen 仍使用原始编译模型，运行时
  仅绘制三配件并按需加载用户改色的 06/16/19 三张贴图。
- 真机结论：该方案仍失败。原因是三角修正之前仍把女仆同名头身参数送进 Sen，而同名参数在
  两个模型中的语义、幅度与方向并不兼容。此版本不得作为后续绑定参考。

以下内容为旧版试验记录，不代表当前架构。

## v0.1.10（呆毛根部单点绑定与头部大幅单轴测试，已废弃）

- v0.1.9 真机确认耳鳍已经正确同向跟随，仅有轻微位移；本轮将耳鳍视为已成功路径，不修改其
  参数映射、九网格筛选、整体旋转、双抖动作或 projection，避免把已通过的部分再次带坏。
- 呆毛仍左右反向，证明公共 Ruby→Sen 头部参数本身并未绑反，问题仅在 Sen 呆毛自身的根部
  响应。新增独立单点绑定：Ruby `Part25=脸` 的当前中心相对中立中心的位移，直接作用到 Sen
  已确认的 `ArtMesh151` 呆毛根部；完全不再读取旧的 `Part9=头饰` 或 `Part70=女仆发带`。
- 单点绑定只对呆毛六网格的独立绘制 pass 追加统一 clip-space 平移，不改变网格内部相对位置，
  因此根部跟随被校正，而尖端弯曲、缩放固化和 Sen 原生物理仍然保留。
- 新增 `头部左右大幅/上下大幅/歪头大幅` 三个单轴测试，分别只写满量程 `AngleX/Y/Z` 及其
  对应物理参数；原“综合头摆”保留。自动巡检扩展为三个头轴、身体、双臂五段循环。
- 静止校准和尾巴绑定全部不变；诊断将呆毛标记为
  `ruby_face_to_ahoge_root_translation`，耳鳍仍为 `shared_parameter_drive_native_rig`。

## v0.1.9（撤销错误头部框架，恢复原生参数跟随）

- 原始模型 `cdi3` 复核确认：v0.1.7/v0.1.8 的头部双点方向锚点并非稳定头骨部件，而是 Ruby
  `Part9=头饰` 与 Sen `Part70=女仆发带`；两套模型对这些装饰物有各自的透视和物理形变，不能
  用它们求同一个刚性头部相似变换。这才是头向右而呆毛/耳鳍向左的根因。
- 完整撤销 v0.1.8 的 X 增量反射，同时撤销 v0.1.7 对呆毛与耳鳍新增的双点 correction。头部
  两组恢复 v0.1.6 的 Ruby→Sen 兼容参数驱动，各自继续使用独立 filter、Sen 原生网格与物理；
  这不是未绑定，而是适用于两套不同拓扑模型的参数级绑定。
- 尾巴不经过上述错误头部路径，继续使用已经真机确认成功的 `Ruby Part29→Part26` 与
  `Sen Part93→Part83` 身体双点修正；位置 `1.00/+0.02/-0.24` 与原生摆动保持不变。
- 所有静止校准完整保留：全局 `1.00/0.00/+0.20`、呆毛 `0.88/-0.03/-0.05`、耳鳍
  `1.24/-0.01/-0.29/整体+5°`。三项选择器、原生双耳九网格、呆毛六网格均不变。
- 诊断明确记录呆毛/耳鳍为 `shared_parameter_drive_native_rig`、尾巴为
  `independent_body_frame_direct`，禁止后续再次把头饰或发带误作刚性头部锚点。

## v0.1.8（头部挂件水平跟随方向修正，已由 v0.1.9 撤销）

- v0.1.7 真机现象：女仆头部向右时，呆毛与耳鳍同步向左。两组都能稳定响应且方向一致，说明
  独立挂件修正已经执行，故障是 Ruby 头部参考 Part 的屏幕 X 增量与可见头部转向相反，并非
  未绑定、网格混组或左右耳再次绑反。
- 仅对呆毛和耳鳍的 Ruby 主模型头部姿势做“当前点相对中立点的 X 增量镜像”，Y 位移、缩放、
  旋转求解、Sen donor 自身姿势抵消和两组独立 projection 全部继续沿用。
- 尾巴复核结果：仍使用 `MAIN Part29→Part26` 与 `Sen Part93→Part83` 的身体双点框架，走直接
  方向且没有经过头部 X 修正；该路径在 v0.1.6 已保持成功，本轮不翻转、不改位置和原生摆动。
- 诊断中的 `attachment_groups` 明确标记头部两组为 `independent_head_frame_x_reflected`，尾巴为
  `independent_body_frame_direct`，便于下一次报告直接确认运行策略。
- 保留 v0.1.7 已确认的绑定零点、原生双耳九网格、呆毛六网格和三项配件选择列表。
- 真机结果仍然反向；结论作废。失败原因不是缺少一次水平翻转，而是头部方向锚点本身选错。

## v0.1.7（三配件独立挂件绑定）

- 用户真机确认 v0.1.6 的网格拆分和显示均正确；新诊断确认呆毛严格为 6 个网格，耳鳍为
  原生左右两组共 9 个网格：`ParamL_angle` 命中右侧 3 个，`ParamR_angle` 命中左侧 6 个，
  `manual_mirror=false`，因此本轮不再修改耳鳍识别与绘制结构。
- 固化最新绑定零位：全局 `1.00/0.00/+0.20`；呆毛 `0.88/-0.03/-0.05`；耳鳍
  `1.24/-0.01/-0.29/整体+5°`；尾巴 `1.00/+0.02/-0.24`（在上一份位置上放大两次）。
- 呆毛、耳鳍、尾巴分别使用自己的过滤器、校准 projection、中立姿势记录和挂件修正调用。
  呆毛与耳鳍虽然都跟随 Ruby 头部框架，但不会共享 drawable filter，也不会生成或绘制组合组；
  呆毛弯曲、左右耳抖动和尾巴摆动仍由各自 Sen 原生网格/物理负责。
- `上一件/下一件` 从枚举全量轮换改为显式三项列表：尾巴 → 呆毛 → 耳鳍。原先第四项是
  `GLOBAL` 整体校准，并非隐藏网格；现已彻底退出配件选择和显示/隐藏操作。
- 诊断新增 `attachment_groups` 和 `calibration_cycle`，明确三组独立绑定且
  `combined_group=false`；若后续某一配件跟随异常，可单独判断而不会与另一头部配件混淆。

## v0.1.6（Sen 原生双耳与呆毛独立驱动）

- 复核 Sen 原项目后确认：原生渲染器只绘制完整模型一次，并没有“复制左耳生成右耳”的逻辑。
  旧版依据当前 X 坐标拆左右耳、再镜像绘制右耳，才是耳鳍间距和旋转失控的根因。
- 耳鳍改为运行时依次拨动 `ParamL_angle/ParamR_angle/ParamR_angle2`，比较可见网格变化，
  从已确认的 `Part113` 六个种子网格扩展出完整原生双耳组；整组每帧只绘制一次。
- 呆毛继续严格限定为 `ArtMesh151/160/189/190/191/192`，不再与耳鳍共用二次头部挂点矩阵；
  两者只接收 Ruby→Sen 的兼容头部参数，各自保留 Sen 内部的变形器和物理响应。
- 尾巴已经通过真机验证，继续保留独立的身体双锚点修正，不改动其网格、动作与默认位置。
- 废弃旧耳鳍镜像产生的对称旋转和间距值，启用新的校准存储键；保留用户最后确认的三组
  X/Y/缩放默认值和耳鳍整组 -10° 校正。
- 修正整体舞台矩阵的乘法顺序：在 projection 后追加同一 clip 变换，使主模型和每个过滤绘制
  使用同一最终缩放/位移，避免整体调整时相对距离被二次放大。
- 诊断升级为 `caicai-maid-accessory-calibration-v2`，新增每个耳鳍参数实际命中的 ArtMesh、
  `manual_mirror=false` 与单次原生双耳绘制模式，便于下一次真机报告精确核验。

## v0.1.5（挂件坐标层与整体缩放轴心修复）

- 真机结果：v0.1.4 尾巴挂件连接成功；呆毛与耳鳍在整体调整时发生大幅漂移，整个女仆模型
  连续缩放时向右下角移动。
- 判定无需再次拆分网格：尾巴成功、头部两组可独立隐藏且耳鳍动力正常，证明三组 drawable
  筛选已经独立。问题来自屏幕空间修正被放进模型投影层后，又经过不同模型的布局矩阵二次换算。
- 新增模型矩阵共轭换算：整体舞台、双锚点挂件、右耳镜像、耳鳍成对旋转与间距全部在最终
  OpenGL clip 坐标层生效，再安全换回各模型的 projection，避免位移和旋转被重复放大。
- 整体缩放轴心从 Live2D 画布原点改为女仆可见立绘中心；主模型与三个配件共用同一轴心，
  缩放时不应再持续向右下角游走。
- 1.0 倍、整体位移为 0 时不执行额外舞台矩阵，保持已经确认的静止校准与尾巴位置不变。
- 诊断新增 `attachment_transform_space=post_model_clip` 及整体缩放轴心 `pivot_x/pivot_y`。

## v0.1.4（三配件双锚点挂件）

- v0.1.3 真机确认耳鳍手动双抖恢复正常后，正式启用三配件动态挂件连接。
- 呆毛与耳鳍共用独立的头部双锚点框架；尾巴使用腰胯/上身双锚点框架。每帧从中立姿势计算
  女仆主模型的平移、旋转和缩放变化，不再只做中心点位移。
- 同时计算 Sen donor 自身的刚性头部/身体框架并将其抵消，避免两套不同 Live2D 参数响应
  叠加造成漂移；框架刻意不取耳鳍、呆毛或尾巴网格，因此耳鳍双抖、呆毛弯曲和尾巴摆动等
  配件内部物理不会被挂点修正抹掉。
- 已确认的 v0.1.2 静止位置继续作为绑定零位；挂件算法在零位为恒等变换，不重新改动用户校准。
- 整体缩放与移动仍先统一应用到主模型和三个配件，再在屏幕空间求挂点修正，防止缩放后漂移、
  消失或重复套用模型矩阵。
- 诊断字段 `attachment_mode` 更新为 `dynamic_two_point_anchor`，便于区分旧固定校准版本。

## v0.1.3（耳鳍独立动力恢复）

- 修复“快速抖动两次”被错误发送给女仆主模型的问题；`Part113` 和兔耳物理属于 Sen
  配件模型，事件现优先路由到 `overlayModel`。
- 修复配件专用更新分支没有推进耳抖计时、也没有在物理计算前准备
  `ParamL_angle/ParamR_angle/ParamR_angle2` 隔离输出的问题。
- 新增仅供配件层使用的最小耳鳍人格循环：不运行 Sen 的表情、身体、触屏或完整待机，只保留
  已确认的两次短脉冲；首次约 24 秒，之后每 28～68 秒低频自主触发，完全静止时暂停。
- 手动按钮与自主反应共用 Sen 已确认的 135%速度、100%幅度和隐藏九轴→独立物理路线。
- 固化 v0.1.2 新诊断位置为重置/干净导入默认值：全局 Y=+0.20；呆毛
  `0.88/-0.02/-0.05`；耳鳍 `1.14/+0.01/-0.29/旋转+8°/间距+0.22/整体-10°`；
  尾巴 `0.96/+0.02/-0.24`。升级安装继续保留已有自动保存值。
- 挂件连接继续延后到耳鳍手动双抖与低频自主反应通过实机确认之后，避免把动力故障和锚点故障
  混在同一轮判断。


## v0.1.2（呆毛/耳鳍硬隔离）

- 复核首轮诊断发现：父级名称虽正确，但 `Part13/Part220` 的递归结果额外夹带十个
  `texture_16` 耳类网格；它们在首轮静止诊断中透明，动作切换后可能显形并被套用呆毛校准，
  从而表现为耳鳍与呆毛合成一个部件。
- 呆毛停止递归 Part 层级，只允许已确认的六段长呆毛网格：
  `ArtMesh151/160/189/190/191/192`。固化根点仍为 `ArtMesh151`，继续保留 Sen 已确认的
  56%整体、100%长度、83%宽度、-49°与模型局部 X=-0.006。
- 耳鳍停止递归邻近头部层级，只允许 `Part113=兔耳` 对应六个网格：
  `ArtMesh631/1095/1021/146/629/1019`；右侧仍由这一组围绕中心轴镜像。
- 增加“单独测试：耳鳍快速抖动两次”，完全复用原 Sen 135%速度、100%幅度的隔离物理路线，
  只混回 `ParamL_angle/ParamR_angle/ParamR_angle2`。若处于完全静止，按钮会先退出静止。
- 本轮不要求新的位置诊断；先用隐藏/显示与耳鳍双抖确认两个配件已经彻底分离。


## v0.1.1（首轮真机修复）

- 修复主模型分前后层绘制时复用并污染投影矩阵的问题；该问题会让第二遍的前层头发重复套用
  模型变换，表现为头顶蓝块、左侧游离发丝，并可能在整体放大后离开裁剪区或黑屏。
- 配件校准位移随整体缩放同比变化，避免呆毛在默认倍率正确、整体缩放后发生相对漂移。
- 固化首轮诊断原始值：全局 `Y -0.01`；呆毛 `0.88 / X -0.02 / Y -0.05`；尾巴
  `0.82 / X 0.00 / Y 0.00`。最终合计位置分别为呆毛 `Y -0.06`、尾巴 `Y -0.01`。
- `Part113` 的六个耳鳍网格全部位于左侧，右侧并非遮挡而是没有独立网格。测试版现在以模型
  中心轴镜像左侧网格生成右耳鳍，左右共用原有物理结果，并继续支持对称旋转、间距和整体旋转。
- 暂停逐帧动态挂点差值修正，本轮使用固定校准变换验证显示、缩放与基础动作；诊断报告新增
  整体舞台变换、挂点模式和右耳鳍来源。

## v0.1.0（第一版测试版）

- 主模型：用户修改后的菜菜女仆；原服装、网格、物理和动作保留。
- Sen 动力层：当前使用完整编译 `.moc3`，只渲染呆毛、`Part113` 耳鳍和 `Part239`
  尾巴；其余 Sen 网格均不进入颜色绘制。
- `Part115` 兔耳结：默认排除；诊断报告单列其子网格，并报告选中配件的遮罩依赖。
- 私有包：单 ZIP + `accessory-lab.json`，App 只需一次导入。
- 贴图：主模型 4 张；Sen 只提供用户改色后的 slot 06/16/19。运行时按选中网格及遮罩依赖
  请求贴图，缺失依赖会提示并写进诊断清单。
- 图层：尾巴 → 主模型低层（含双马尾）→ 耳鳍 → 主模型高层 → 呆毛。
- 挂点：复制双方共有的头部、身体和呼吸参数；叠加主模型与配件模型的逐帧锚点位移差修正。
- 校准：三配件均有 X/Y/缩放/显示；耳鳍另有对称旋转、对称间距、成对中心轴旋转。
- 静止：暂停待机、测试运动、物理推进与点击反馈，作为位置诊断零点。
- 诊断：导出校准数值、配件网格、父 Part、贴图槽、遮罩源和 `Part115` 状态。
- 主模型交互：7 个互斥表情、7 个互斥动作；白袜/丝袜带子/双马尾/发带可独立切换；
  “变小”可与所有组叠加；点击预设仅在非静止状态触发。
- 袜子：默认黑袜，按钮初始显示“白袜”；切到白袜后按钮显示“黑袜”。
- 颜色：`ArtMesh122` 与 `ArtMesh149` 强制为 `#9E9EB2`；Sen 三配件使用用户已改色贴图，
  不叠加旧 Sen ArtMesh 色表。

## 后续验证

1. 真机依次运行头部摆动、身体摆动、变小和自动巡检，检查头部两配件与尾巴的跟随方向、
   旋转幅度和缩放比例。
2. 在挂件连接启用时再次触发耳鳍双抖，确认局部物理没有被刚性锚点抵消。
3. 测试奶茶、菜单、左右餐盘等大幅姿势，并观察尾巴是否仍稳定连接腰胯。
4. 若某一组出现持续偏差，再导出当前版本 JSON；静止位置无误时无需重新校准。
5. 用户提供原始 `.cmo3` 后，在 Cubism Editor 中删除无关 ArtMesh/Deformer/纹理并重新导出精简
   donor `.moc3`；保持文件名和 manifest 路径不变即可替换。
