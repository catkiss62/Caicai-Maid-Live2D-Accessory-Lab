--- a/framework/src/main/java/com/live2d/sdk/cubism/framework/rendering/android/CubismRendererAndroid.java
+++ b/framework/src/main/java/com/live2d/sdk/cubism/framework/rendering/android/CubismRendererAndroid.java
@@ -261,6 +261,11 @@
         drawableVisibilityFilter = enabled == null ? null : enabled.clone();
     }
 
+    /** Bypasses authored clip masks only for the selected drawable color pass. */
+    public void setDrawableMaskBypassFilter(boolean[] enabled) {
+        drawableMaskBypassFilter = enabled == null ? null : enabled.clone();
+    }
+
     /**
      * Get textures list bound to OpenGL.
      *
@@ -694,6 +699,11 @@
         CubismClippingContextAndroid clipContext = (drawableClippingManager != null)
             ? drawableClippingManager.getClippingContextListForDraw().get(drawableIndex)
             : null;
+        if (drawableMaskBypassFilter != null && drawableIndex >= 0
+            && drawableIndex < drawableMaskBypassFilter.length
+            && drawableMaskBypassFilter[drawableIndex]) {
+            clipContext = null;
+        }
 
         // マスクを描く必要がある
         if (clipContext != null && isUsingHighPrecisionMask()) {
@@ -1148,6 +1158,7 @@
     private Map<Integer, Integer> cachedImmutableTextures;
 
     private boolean[] drawableVisibilityFilter;
+    private boolean[] drawableMaskBypassFilter;
 
     /**
      * A array of drawing object indices arranged in drawing order.
